#pragma once

#include <string>

struct Contact {
    std::string firstName;
    std::string lastName;
    std::string mobileNumber;
    std::string homeNumber;
    std::string workNumber;
    std::string email;
};
